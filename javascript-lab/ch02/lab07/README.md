# JSON과 로컬 저장

JSON과 `localStorage`를 이용하여 데이터를 저장·복원하고, 저장된 데이터가 없거나 잘못된 경우를 처리한다.

- `JSON.stringify`, `JSON.parse`
- `localStorage`
- `try...catch` 
- 초기 데이터 처리

## 실습

변경 내용 저장, 새로고침 후 복원, 잘못된 저장 데이터 처리