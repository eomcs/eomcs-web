# HTTP와 fetch

HTTP 요청·응답과 동일 출처·CORS의 기본 개념을 이해하고, `fetch`로 JSON 데이터를 조회하며 `response.ok`로 HTTP 오류 응답을 판별한다.

- 요청·응답, GET·POST의 역할, 상태 코드
- `response.ok`, `response.json()`
- Network 탭
- 동일 출처와 CORS 개념

## 실습

로컬 HTTP 서버의 JSON 파일 조회, 404 응답 처리
